# Postgres connection string. On Vercel, use the Neon "pooled" connection
# string (it contains `-pooler`). Locally, paste the same value to develop
# against the cloud DB, or run a local Postgres.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
